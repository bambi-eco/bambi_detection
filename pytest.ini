[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: needs downloaded flight data or model weights (deselect with -m "not slow")
    notebook: executes a notebook end to end
filterwarnings =
    ignore::DeprecationWarning

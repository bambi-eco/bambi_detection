# Rendered notebooks

Executed copies of `../*.ipynb` with their outputs (plots, printed numbers),
so the results on the public BAMBI flights can be read here on GitHub without
running anything. Regenerate with `python notebooks/render_all.py`; the
sources one level up stay output-free and are what CI executes.
